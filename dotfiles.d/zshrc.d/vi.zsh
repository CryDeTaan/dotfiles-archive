# If I need any specific zsh vi-mode mappings, I can set them here.

bindkey -v

# Not sure which is the more correct way to set this so I have both
#bindkey "jj" vi-cmd-mode
bindkey -M viins 'jj' vi-cmd-mode

